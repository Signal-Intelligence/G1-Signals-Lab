/** Shared typography for card header status lines (sys status, agent trust). */
export const HEADER_META_TEXT =
  "text-[9px] font-normal uppercase tracking-telemetry";

export const HEADER_META_MUTED = `${HEADER_META_TEXT} text-text-muted`;

export const HEADER_META_SEPARATOR = "text-[9px] text-text-data";

/** Dot width (w-1.5) + gap (gap-1.5) — aligns agent text with the S in SYS */
export const HEADER_META_TEXT_INDENT = "pl-3";
